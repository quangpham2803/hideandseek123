using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
public class AddMinimapToPlayer : MonoBehaviourPunCallbacks
{
    //[SerializeField] MiniMapComponent miniMapComponent;
    //// Start is called before the first frame update
    //public void Start()
    //{
    //    StartCoroutine(wait());
    //}
    //bool CheckHadRole()
    //{
    //    return GameManager.instance.state == GameManager.GameState.Starting;
    //}
    //IEnumerator wait()
    //{
    //    yield return new WaitUntil(CheckHadRole);
    //    if (photonView.IsMine)
    //    {
    //        MiniMapController.minimapController.target = this.transform;
    //    }
    //    if (GetComponent<PlayerSetup>().player == GameManager.instance.mainPlayer.GetComponent<PlayerSetup>().player)
    //    {
    //        miniMapComponent.enabled = true;
    //    }
    //    else
    //    {
    //        miniMapComponent.enabled = false;
    //    }
    //}
}
